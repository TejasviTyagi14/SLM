__pycache__/
*.pyc
.env
.venv/
venv/
results/**/*.jsonl
.DS_Store

# rxndata pipeline: raw downloads + per-phase interim are cached/DVC-tracked, not committed.
data/raw/
data/interim/
# Cloned upstream repo (reproduced by `make setup`), not vendored into git history.
third_party/oMeBench/
# But DO keep the directory scaffolding + curated reason-code READMEs.
!data/quarantine/
!data/rejects/
!data/final/
*.egg-info/
build/
dist/

# Phase 8 generated corpora (regenerable via `make phase8`); keep data card + reports.
data/final/*.jsonl
data/final/pretrain_corpus.txt

# HF export preview (regenerable via scripts/export_to_hf.py --dry-run)
data/final/hf_export/
